import localFont from "next/font/local";

export const clashDisplay = localFont({
  src: "./ClashDisplay-Regular.woff",
  variable: "--font-clash-display",
});
/* export const clashDisplay_ = Clash_Display({
  src: "../public/fonts/ClashDisplay-Regular.woff2", // Adjust path as needed
  variable: "--font-clash-display", // Optional: Define a CSS variable
  display: "swap", // Optional: Control font loading behavior
}); */
